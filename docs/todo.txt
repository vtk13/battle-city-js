+ баг в игре 2 на 2 когда двое появляются в одном месте
+/- анимация (мигание базы перед окончанием защиты, ...)
+ в firefox кнопка f открывает окно поиска (стрелять переделанно на пробел)
+ различные элементы ландшафта (кирпич, сталь, деревья, вода, лед)
+ бонусы
- вынести танк пользователя в отдельный класс
+ определенное кол-во ботов и их появление в разных местах
+ жизни игроков
+/- очки
    отменять накопленные за уровень очки, если уровень проигран
+ уровни
+ разделить синхронизацию premade на сообщения и пользователей.
    пользователи прямо в premade (--так как нельзя сделать отдельно список для пользователей--)
- запретить логин с уже авторизованным ником
+ выбор игры кликом в списке
+ подсветить себя в чате
+ столкновение пуль, полет пули можно представить в виде прямоугольника-следа
  вообще проблема пересечения объектов при слишком большой скорости
+ игрок не удаляется при disconnect если он играл
+ возможно будет логичнее для изменения объектов на клиенте всегда использовать
  событие sync, и не делать это ни по каким другим событиям (например joined)
- синхронизируются все данные, а не только необходимые.
- say is a user's method
- Избавиться от событий, перейти на АОП?
- Не хорошо изменять свойства других объектов.
- serialize - не совсем serialize, это подготовка данных для клиента.
- жесть при потыки зайти в уже идущую игру
- можно сделать кучу настроек для игры, типа "враги могут подбирать бонусы"
- onBonus vs Bonus.applyTo?
- unserialize for all serialize
- https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Function/bind
- Child.prototype.__proto__ = Parent.prototype
- <bc.rj12.ru> vtk: по поводу передачи бинарных данных, если кодировать их в base64 и передавать должно выйти около 16 байт
- 15:45 <vtk > не думал как проксировать на 80 порт
  15:50 <vtk> да не проблема. у меня он просто занят.
  15:51 <vtk> а как через апач проксировать - нет, не думал
  15:51 <vtk > через haproxy
  15:53 <vtk > ставишь haproxy перед апачем, но лучше перед жниксом проксируешь вебсокеты на ноду а http на апач или жникс
  15:58 <vtk> вроде интересная штука - haproxy, надо будет разобраться. а нет статейки про haproxy имено по моему случаю?
  16:00 <vtk > http://storage8.static.itmages.ru/i/11/1126/h_1322301626_3909609_b14c38f5d2.png
  16:01 <vtk > есть такая статейка http://artursabirov.ru/2011/10/19/assync-servers
- Можно время не уменьшать так намного, а выдавать пользователям сообщение о Победе/Проигрыше. Таким, чтобы оно заметно было.