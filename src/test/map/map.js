define([
    'assert',
    'src/map/map.js'
], function(
    assert,
    Map
) {
    describe('Map', function() {
        var map;

        beforeEach(function() {
            map = new Map();
        });

        it('ok', function(done) {
            done();
        });
    });
});
